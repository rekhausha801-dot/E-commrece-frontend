Created At: 2026-08-06T04:35:56Z
Completed At: 2026-08-06T04:35:58Z
The following changes were made by the multi_replace_file_content tool to: d:\n\E-commrece-frontend\src\pages\admin\Dashboard.jsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -472,81 +472,6 @@
 
 
         
-          <div className="dashboard-card" style={{ padding: '24px', backgroundColor: '#fcfaf5', borderRadius: '16px', border: '1px solid #f2ecdf', boxShadow: '0 4px 20px rgba(0,0,0,0.02)' }}>
-            <div className="card-header" style={{ marginBottom: '24px', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
-              <h3 style={{ margin: 0, fontSize: '16px', fontWeight: '600', color: '#1a1a1a', fontFamily: '"Playfair Display", Georgia, serif', whiteSpace: 'nowrap' }}>Top Selling Products</h3>
-              <button className="card-action" style={{ display: 'flex', alignItems: 'center', color: '#555', fontSize: '12px', fontWeight: '400', background: 'none', border: 'none', cursor: 'pointer', whiteSpace: 'nowrap' }}>View All</button>
-            </div>
-            <div className="top-selling-list" style={{ display: 'flex', flexDirection: 'column', flex: 1, justifyContent: 'space-between' }}>
-              {topSellingData.filter(item => item.name.toLowerCase().includes(searchQuery.toLowerCase())).map((item, index) => (
-                <div key={index} style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', paddingBottom: index !== topSellingData.length - 1 ? '16px' : '0', marginBottom: index !== topSellingData.length - 1 ? '16px' : '0', borderBottom: index !== topSellingData.length - 1 ? '1px dashed #e6dec9' : 'none' }}>
-                  <div style={{ display: 'flex', alignItems: 'center', gap: '20px' }}>
-                    <div style={{ width: '52px', height: '52px', borderRadius: '50%', overflow: 'hidden', backgroundColor: '#fff', flexShrink: 0, border: '1px solid #eadecb', padding: '2px' }}>
-                      <img src={item.img} alt={item.name} style={{ width: '100%', height: '100%', objectFit: 'cover', borderRadius: '50%' }} />
-                    </div>
-                    <div style={{ display: 'flex', flexDirection: 'column', gap: '2px' }}>
-                      <span style={{ fontSize: '16px', fontWeight: '500', color: '#111', fontFamily: '"Playfair Display", Georgia, serif' }}>{item.name}</span>
-                      <span style={{ fontSize: '13px', color: '#888', fontWeight: '400' }}>{item.sold}</span>
-                    </div>
-                  </div>
-                  <span style={{ fontSize: '14px', fontWeight: '500', color: '#111', letterSpacing: '0.5px' }}>{item.price}</span>
-                </div>
-              ))}
-            </div>
-          </div>
-
-        </div>
-
-
-        <div className="bottom-grid">
-
-       
-          <div className="dashboard-card" style={{ padding: '24px' }}>
-            <div className="card-header" style={{ marginBottom: '24px' }}>
-              <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
-                <div style={{ width: '40px', height: '40px', borderRadius: '12px', backgroundColor: '#fdf8ee', display: 'flex', alignItems: 'center', justifyContent: 'center', boxShadow: '0 4px 10px rgba(201,160,91,0.15)' }}>
-                  <ShoppingBag size={20} color="#c9a05b" />
-                </div>
-                <h3 style={{ margin: 0, fontSize: '16px', fontWeight: '600', color: '#222' }}>Recent Orders</h3>
-              </div>
-              <button className="card-action" style={{ display: 'flex', alignItems: 'center', gap: '4px', color: '#c9a05b', fontWeight: '500' }}>View All <ArrowRight size={14} /></button>
-            </div>
-            <div className="recent-orders-list" style={{ display: 'flex', flexDirection: 'column' }}>
-              {recentOrdersData.filter(order => order.id.toLowerCase().includes(searchQuery.toLowerCase()) || order.customer.toLowerCase().includes(searchQuery.toLowerCase())).map((order, index) => (
-                <div className="recent-order-item" key={index} style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', paddingBottom: '20px', marginBottom: index !== recentOrdersData.length - 1 ? '20px' : '0', borderBottom: index !== recentOrdersData.length - 1 ? '1px solid #f0eade' : 'none' }}>
-                  <div style={{ display: 'flex', alignItems: 'center', gap: '16px' }}>
-                    <div style={{ width: '64px', height: '64px', borderRadius: '12px', overflow: 'hidden', backgroundColor: '#f5f5f5' }}>
-                      <img src={order.img} alt="Product" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
-                    </div>
-                    <div style={{ display: 'flex', flexDirection: 'column', gap: '4px' }}>
-                      <span style={{ fontSize: '14px', fontWeight: '600', color: '#222' }}>{order.id}</span>
-                      <span style={{ fontSize: '12px', color: '#888' }}>{order.date}</span>
-                      <div style={{ display: 'flex', alignItems: 'center', gap: '6px', marginTop: '4px' }}>
-                        <User size={12} color="#888" />
-                        <span style={{ fontSize: '12px', color: '#666' }}>{order.customer}</span>
-                      </div>
-                    </div>
-                  </div>
-                  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'flex-end', gap: '10px' }}>
-                    <span style={{ fontSize: '16px', fontWeight: '700', color: '#222' }}>{order.amount}</span>
-                    <div style={{ display: 'flex', alignItems: 'center', gap: '6px', padding: '4px 10px', borderRadius: '6px', backgroundColor: order.status === 'Delivered' ? '#e8f5e9' : order.status === 'Processing' ? '#e3f2fd' : '#fff4e5' }}>
-                      <div style={{ width: '6px', height: '6px', borderRadius: '50%', backgroundColor: order.status === 'Delivered' ? '#4caf50' : order.status === 'Processing' ? '#2196f3' : '#ff9800' }}></div>
-                      <span style={{ fontSize: '12px', fontWeight: '500', color: order.status === 'Delivered' ? '#4caf50' : order.status === 'Processing' ? '#2196f3' : '#ff9800' }}>{order.status}</span>
-                    </div>
-                  </div>
-                </div>
-              ))}
-            </div>
-            <div style={{ marginTop: 'auto', paddingTop: '24px' }}>
-              <button style={{ width: '100%', padding: '14px', backgroundColor: '#fdf8ee', border: 'none', borderRadius: '8px', color: '#c9a05b', fontWeight: '600', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '8px', cursor: 'pointer', fontSize: '14px' }}>
-                <ShoppingBag size={16} /> View All Orders <ArrowRight size={16} style={{ marginLeft: '4px' }} />
-              </button>
-            </div>
-          </div>
-
-
-
-         
           <div className="dashboard-card" style={{ padding: '24px' }}>
             <div className="card-header" style={{ marginBottom: '24px' }}>
               <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
@@ -590,6 +590,81 @@
               </button>
             </div>
           </div>
+
+        </div>
+
+
+        <div className="bottom-grid">
+
+       
+          <div className="dashboard-card" style={{ padding: '24px' }}>
+            <div className="card-header" style={{ marginBottom: '24px' }}>
+              <div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
+                <div style={{ width: '40px', height: '40px', borderRadius: '12px', backgroundColor: '#fdf8ee', display: 'flex', alignItems: 'center', justifyContent: 'center', boxShadow: '0 4px 10px rgba(201,160,91,0.15)' }}>
+                  <ShoppingBag size={20} color="#c9a05b" />
+                </div>
+                <h3 style={{ margin: 0, fontSize: '16px', fontWeight: '600', color: '#222' }}>Recent Orders</h3>
+              </div>
+              <button className="card-action" style={{ display: 'flex', alignItems: 'center', gap: '4px', color: '#c9a05b', fontWeight: '500' }}>View All <ArrowRight size={14} /></button>
+            </div>
+            <div className="recent-orders-list" style={{ display: 'flex', flexDirection: 'column' }}>
+              {recentOrdersData.filter(order => order.id.toLowerCase().includes(searchQuery.toLowerCase()) || order.customer.toLowerCase().includes(searchQuery.toLowerCase())).map((order, index) => (
+                <div className="recent-order-item" key={index} style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', paddingBottom: '20px', marginBottom: index !== recentOrdersData.length - 1 ? '20px' : '0', borderBottom: index !== recentOrdersData.length - 1 ? '1px solid #f0eade' : 'none' }}>
+                  <div style={{ display: 'flex', alignItems: 'center', gap: '16px' }}>
+                    <div style={{ width: '64px', height: '64px', borderRadius: '12px', overflow: 'hidden', backgroundColor: '#f5f5f5' }}>
+                      <img src={order.img} alt="Product" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
+                    </div>
+                    <div style={{ display: 'flex', flexDirection: 'column', gap: '4px' }}>
+                      <span style={{ fontSize: '14px', fontWeight: '600', color: '#222' }}>{order.id}</span>
+                      <span style={{ fontSize: '12px', color: '#888' }}>{order.date}</span>
+                      <div style={{ display: 'flex', alignItems: 'center', gap: '6px', marginTop: '4px' }}>
+                        <User size={12} color="#888" />
+                        <span style={{ fontSize: '12px', color: '#666' }}>{order.customer}</span>
+                      </div>
+                    </div>
+                  </div>
+                  <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'flex-end', gap: '10px' }}>
+                    <span style={{ fontSize: '16px', fontWeight: '700', color: '#222' }}>{order.amount}</span>
+                    <div style={{ display: 'flex', alignItems: 'center', gap: '6px', padding: '4px 10px', borderRadius: '6px', backgroundColor: order.status === 'Delivered' ? '#e8f5e9' : order.status === 'Processing' ? '#e3f2fd' : '#fff4e5' }}>
+                      <div style={{ width: '6px', height: '6px', borderRadius: '50%', backgroundColor: order.status === 'Delivered' ? '#4caf50' : order.status === 'Processing' ? '#2196f3' : '#ff9800' }}></div>
+                      <span style={{ fontSize: '12px', fontWeight: '500', color: order.status === 'Delivered' ? '#4caf50' : order.status === 'Processing' ? '#2196f3' : '#ff9800' }}>{order.status}</span>
+                    </div>
+                  </div>
+                </div>
+              ))}
+            </div>
+            <div style={{ marginTop: 'auto', paddingTop: '24px' }}>
+              <button style={{ width: '100%', padding: '14px', backgroundColor: '#fdf8ee', border: 'none', borderRadius: '8px', color: '#c9a05b', fontWeight: '600', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '8px', cursor: 'pointer', fontSize: '14px' }}>
+                <ShoppingBag size={16} /> View All Orders <ArrowRight size={16} style={{ marginLeft: '4px' }} />
+              </button>
+            </div>
+          </div>
+
+
+
+         
+          <div className="dashboard-card" style={{ padding: '24px', backgroundColor: '#fcfaf5', borderRadius: '16px', border: '1px solid #f2ecdf', boxShadow: '0 4px 20px rgba(0,0,0,0.02)' }}>
+            <div className="card-header" style={{ marginBottom: '24px', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
+              <h3 style={{ margin: 0, fontSize: '16px', fontWeight: '600', color: '#1a1a1a', fontFamily: '"Playfair Display", Georgia, serif', whiteSpace: 'nowrap' }}>Top Selling Products</h3>
+              <button className="card-action" style={{ display: 'flex', alignItems: 'center', color: '#555', fontSize: '12px', fontWeight: '400', background: 'none', border: 'none', cursor: 'pointer', whiteSpace: 'nowrap' }}>View All</button>
+            </div>
+            <div className="top-selling-list" style={{ display: 'flex', flexDirection: 'column', flex: 1, justifyContent: 'space-between' }}>
+              {topSellingData.filter(item => item.name.toLowerCase().includes(searchQuery.toLowerCase())).map((item, index) => (
+                <div key={index} style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', paddingBottom: index !== topSellingData.length - 1 ? '16px' : '0', marginBottom: index !== topSellingData.length - 1 ? '16px' : '0', borderBottom: index !== topSellingData.length - 1 ? '1px dashed #e6dec9' : 'none' }}>
+                  <div style={{ display: 'flex', alignItems: 'center', gap: '20px' }}>
+                    <div style={{ width: '52px', height: '52px', borderRadius: '50%', overflow: 'hidden', backgroundColor: '#fff', flexShrink: 0, border: '1px solid #eadecb', padding: '2px' }}>
+                      <img src={item.img} alt={item.name} style={{ width: '100%', height: '100%', objectFit: 'cover', borderRadius: '50%' }} />
+                    </div>
+                    <div style={{ display: 'flex', flexDirection: 'column', gap: '2px' }}>
+                      <span style={{ fontSize: '16px', fontWeight: '500', color: '#111', fontFamily: '"Playfair Display", Georgia, serif' }}>{item.name}</span>
+                      <span style={{ fontSize: '13px', color: '#888', fontWeight: '400' }}>{item.sold}</span>
+                    </div>
+                  </div>
+                  <span style={{ fontSize: '14px', fontWeight: '500', color: '#111', letterSpacing: '0.5px' }}>{item.price}</span>
+                </div>
+              ))}
+            </div>
+          </div>
         </div>
 
       
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.